const SCHEMA = 'salesforce.';

export const TABLE = {
    VEHICLE: SCHEMA + 'vehicle__c',
    RESERVATION: SCHEMA + 'Reservations__c',
    TX: SCHEMA + 'Transactions_NTTA__c',
    CONTACT: SCHEMA + 'Contact'
}